micronaut:
  application:
    name: customer
datasources:
  default:
    url: jdbc:postgresql://${JDBC_HOST}:${JDBC_PORT}/postgres
    username: postgres
    password: qwe
    driver-class-name: org.postgresql.Driver
    db-type: postgres
    schema-generate: CREATE_DROP
    dialect: POSTGRES
jpa.default.properties.hibernate:
  hbm2ddl.auto: validate
  show_sql: true
flyway:
  datasources:
    default:
      enabled: true

      JDBC_URL: jdbc:postgresql://db:5432/time
      JDBC_HOST: db
      JDBC_PORT: 5432
      JDBC_USER: postgres
      JDBC_PASSWORD: qwe